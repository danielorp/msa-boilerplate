apiVersion: v1
kind: Service
metadata:
  name: {{ include "tms-services.fullname" . }}
  labels:
    {{- include "tms-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tms-services.selectorLabels" . | nindent 4 }}
