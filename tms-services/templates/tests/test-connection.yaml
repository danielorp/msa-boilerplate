apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tms-services.fullname" . }}-test-connection"
  labels:
    {{- include "tms-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tms-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
